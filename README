Installation:
	git clone https://github.com/DanielGreth/dotvim.git ~/.vim

Create symlink:
	ln -s ~/.vim/vimrc ~/.vimrc

Switch to the '~/.vim' drectory, and fetch submodules:
	cd ~/.vim
	git submodule init
	git submodule update
